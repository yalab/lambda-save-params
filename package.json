{
  "name": "lambda-save-params",
  "version": "0.0.1",
  "description": "AWS Lambda function for save any webhook request to S3.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.2.31",
    "moment": "^2.11.1",
    "s3": "^4.4.0"
  }
}
